apiVersion: constraints.gatekeeper.sh/v1beta1
kind: Example
metadata:
  name: example-policy
spec:
  enforcementAction: dryrun
  match:
    kinds:
      - apiGroups: [""]
        kinds: [""]
    excludedNamespaces:
      - kube-system
      - gatekeeper-system
