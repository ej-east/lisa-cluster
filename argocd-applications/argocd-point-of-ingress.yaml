apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: point-of-ingress
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  source:
    path: manifests/ingress/
    repoURL: https://github.com/ej-east/lisa-cluster
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
