apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-lisa
spec:
  secretName: wildcard-lisa-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: "*.lisa"
  dnsNames:
    - "*.lisa"
    - lisa