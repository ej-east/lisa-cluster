apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: memos-db
  namespace: memo
spec:
  instances: 1
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: memos
      owner: memos
      secret:
        name: memos-secret
