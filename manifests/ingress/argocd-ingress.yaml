apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: argocd
  namespace: argocd

spec:
  entryPoints:
    - webSecure
  routes:
  - kind: Rule
    match: HostSNI(`argo.lisa`)

    services:
      - name: argocd-server
        port: 443
  tls:
    passthrough: true