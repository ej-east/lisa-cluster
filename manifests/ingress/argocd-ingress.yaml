apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: argocd
  namespace: argocd

spec:
  entryPoints:
    - websecure
  routes:
  - kind: Rule
    match: HostSNI(`argo.lisa`)

    services:
      - name: argocd-server
        port: 443
  tls: {}