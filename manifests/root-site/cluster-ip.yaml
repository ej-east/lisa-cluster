apiVersion: v1
kind: Service
metadata:
  name: root-site
  namespace: root-site
spec:
  selector:
    app: root-site
  type: ClusterIP
  ports:
    - name: root-site
      protocol: TCP
      port: 80
      targetPort: 80
